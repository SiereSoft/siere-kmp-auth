# OpenID Connect on JVM desktop

The OpenID Connect documentation now lives with the provider module:

**[Open the `auth-oidc` setup guide](../auth-oidc/README.md).**

This compatibility page keeps existing links working.
